name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: 
      - main
jobs:
  rust-build:
    runs-on: ubuntu-latest
    container: 
      image: rust
      volumes:
        - /home/runner/work/rocket/rocket/:/rust/src
    steps:
      - name: check
        run: |
          pwd && \
          ls -al
      - name: cd
        run: |
          cd && \
          pwd && \
          ls -al
      - name: check-2
        run: |
          pwd && \
          ls -al
      - name: cargo-build
        run: |
          cargo build --release