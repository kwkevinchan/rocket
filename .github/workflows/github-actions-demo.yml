name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: 
      - main
jobs:
  rust-build:
    runs-on: ubuntu-latest
    container: rust
      volumes:
        - /home/runner/work/rocket/rocket/:/home/rust/src
      working-directory: /home/rust/src
    steps:
      - name: check
        run: |
          pwd && \
          ls -al
      - name: cargo-build
        run: |
          cargo build --release