name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: 
      - main
jobs:
  rust-build:
    runs-on: ubuntu-latest
    container: 
      image: rust
      volumes:
        - /home/runner/work/rocket/rocket/:/__w/rocket/rocket
    steps:
      - name: cargo-build
        run: |
          cargo build --release